<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BrawlInstaller"
        xmlns:viewModels="clr-namespace:BrawlInstaller.ViewModels"
        x:Class="BrawlInstaller.MainWindow"
        Title="BrawlInstaller"
        Width="1280"
        Height="720">

    <Grid>
        <ContentControl x:Name="MainView"
                        Content="{Binding MainViewModel}"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch"/>
    </Grid>
</Window>